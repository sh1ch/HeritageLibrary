<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 余白 -->
    <!--
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/HeritageLibrary.Wpf.DesignSystem;component/Themes/Margin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    -->

    <Style x:Key="H1" TargetType="{x:Type Border}">
        <Setter Property="Margin" Value="0 64 0 24"/>
    </Style>

    <Style x:Key="H2" TargetType="{x:Type Border}">
        <Setter Property="Margin" Value="0 64 0 24"/>
    </Style>

    <Style x:Key="H3" TargetType="{x:Type Border}">
        <Setter Property="Margin" Value="0 40 0 24"/>
    </Style>

    <Style x:Key="H4" TargetType="{x:Type Border}">
        <Setter Property="Margin" Value="0 40 0 16"/>
    </Style>

    <Style x:Key="H5" TargetType="{x:Type Border}">
        <Setter Property="Margin" Value="0 40 0 16"/>
    </Style>

    <Style x:Key="H6" TargetType="{x:Type Border}">
        <Setter Property="Margin" Value="0 24 0 16"/>
    </Style>

</ResourceDictionary>